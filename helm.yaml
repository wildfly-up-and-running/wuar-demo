image:
  name: ghcr.io/wildfly-up-and-running/wuar-demo
build:
  enabled: false
deploy:
  route:
    enabled: false
  env:
    - name: DEMO_DEFAULT_TEXT
      value: Hello from Kubernetes!
    # temporary workaround to be able to port-forward
    - name: SERVER_PUBLIC_BIND_ADDRESS
      value: 0.0.0.0
